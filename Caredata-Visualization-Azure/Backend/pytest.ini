[pytest]
testpaths = tests
markers =
    slow: long-running tests (e.g. requiring downloaded ML models)
filterwarnings =
    ignore::DeprecationWarning:jose.*
    ignore::pydantic.PydanticDeprecatedSince20
